[test]
# Coverage reports reflect source accuracy; test files themselves are excluded.
# No hard threshold yet — the imported Gmail canary sets the initial baseline;
# a per-workspace target is raised deliberately once services stabilize.
coverageSkipTestFiles = true
