{
  "name": "google-mcp-suite",
  "private": true,
  "type": "module",
  "description": "Per-account, per-service Google MCP servers sharing one auth implementation.",
  "author": "the-simian <info@simiancraft.com>",
  "license": "MIT",
  "homepage": "https://github.com/simiancraft/google-mcp-suite#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simiancraft/google-mcp-suite.git"
  },
  "bugs": {
    "url": "https://github.com/simiancraft/google-mcp-suite/issues"
  },
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write .",
    "typecheck": "bun run --filter './packages/*' typecheck && bun run --filter './services/*' typecheck",
    "build": "bun run --filter './packages/*' build && bun run --filter './services/*' build",
    "test": "bun test",
    "check:knip": "knip",
    "check": "bun run lint:fix && bun run build && bun run typecheck && bun run test && bun run check:knip"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/addressparser": "^1.0.3",
    "@types/bun": "^1.3.13",
    "fast-check": "^4.8.0",
    "knip": "^6.14.0",
    "typescript": "^6.0.0"
  }
}
