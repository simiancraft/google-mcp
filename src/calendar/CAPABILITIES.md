# Calendar capabilities

<!-- Generated by `bun run capabilities`; do not edit by hand. -->

17 operations across MCP tools and REST methods. ⚠️ marks irreversible operations (MCP `destructiveHint`).

| Operation | Source | Description |
| --- | --- | --- |
| `list_events` | MCP Tool | List events on a calendar, optionally filtered, ordered, and paginated. |
| `get_event` | MCP Tool | Get a single event from a calendar by id. |
| `list_calendars` | MCP Tool | List the calendars on the user's calendar list, paginated. |
| `suggest_time` | MCP Tool | Suggest free time slots where the given attendees can meet within a time window. |
| `create_event` | MCP Tool | Create an event on a calendar. |
| `update_event` | MCP Tool | Update fields of an existing event; unset fields are left unchanged. |
| `delete_event` ⚠️ | MCP Tool | Delete an event from a calendar, returning the deleted event. |
| `respond_to_event` | MCP Tool | Respond to an event invitation: declined, tentative, or accepted. |
| `list_event_instances` | REST Method | List the instances of a recurring event, optionally filtered and paginated. |
| `move_event` | REST Method | Move an event to another calendar; only default-type events can move. |
| `quick_add_event` | REST Method | Create an event from a natural-language description. |
| `patch_event` | REST Method | Patch any REST field of an event, including those update_event omits; unset fields are left unchanged. |
| `get_calendar` | REST Method | Get a calendar (its title, description, location, and time zone) by id. |
| `create_calendar` | REST Method | Create a secondary calendar. |
| `update_calendar` | REST Method | Update a calendar's title, description, location, and/or time zone; unset fields are left unchanged. |
| `delete_calendar` ⚠️ | REST Method | Permanently delete a secondary calendar and all of its events. |
| `clear_calendar` ⚠️ | REST Method | Permanently delete all events from the primary calendar. |
